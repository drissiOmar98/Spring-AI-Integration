spring:
  application:
    name: structured-output
  ai:
    openai:
      api-key: ${OPEN_AI_API_KEY}
      chat:
        options:
          model: gpt-4o


logging:
  level:
    org.springframework.ai.chat.client.advisor: DEBUG