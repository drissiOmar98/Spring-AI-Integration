# 🌿 Spring AI - Structured & Unstructured Vacation Plan Generator
## AI-Powered Itinerary Generation with Strongly Typed Outputs

This project demonstrates how to use **Spring AI** to generate both **unstructured text** and **strongly typed structured JSON** using AI models.  
Through the `VacationPlanController`, the application showcases two patterns:

1. **Unstructured AI output** → raw text responses
2. **Structured AI output** → automatically mapped Java records (`Itinerary`, `Activity`)

This approach highlights how Spring AI can integrate AI-generated data directly into type-safe Spring Boot applications.

---

## ✨ Key Features

### 📝 Unstructured Output (`/vacation/unstructured`)
- Returns **plain text** generated by the AI model.
- Best for quick suggestions, prototyping, casual prompts, and simple scenarios.
- No data structure—just raw AI content.

---

### 📊 Structured Output (`/vacation/structured`)
- Uses Spring AI’s **entity mapping** to convert AI responses directly into Java records.
- Automatically maps the response into:
  ```java
  public record Itinerary(List<Activity> itinerary) {}
  public record Activity(String activity, String location, String day, String time) {}

## 📋 Requirements

Ensure you have the following installed:

- **Java 21+**
- **Maven or Gradle**
- **API key** for your chosen AI provider  
  (OpenAI-compatible LLMs or local model via Spring AI)

---

## ⚙️ Configuration

Configure your AI provider and API key using environment variables.  
Example for OpenAI:

```bash
export OPENAI_API_KEY=YOUR_TOKEN_VALUE_HERE