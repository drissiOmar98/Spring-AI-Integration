### 🔹 ChatController – Non-streaming POST endpoint
# Sends a user prompt to the AI model and returns the full response in one shot.
POST http://localhost:8080/api/chat
Content-Type: application/json

{
  "prompt": "What is Spring Boot?"
}

### 🔹 StreamController – Full response GET endpoint
# Returns the complete AI response in one request (non-streaming).

GET http://localhost:8080/api/stream/full?message=Give me 5 Spring Boot best practices
Content-Type: application/json



### 🔹 StreamController – Streaming SSE endpoint
# Streams the AI response token-by-token (Server-Sent Events), ideal for real-time UI.
GET http://localhost:8080/api/stream/sse?message=Stream 5 tips to improve Spring AI applications
Accept: text/event-stream


### 🔹 AcmeBankController – Valid banking-related question
# Sends a banking-related question to the AI assistant.
# The AI responds only to banking queries as defined in system instructions.
GET http://localhost:8080/acme/chat?message=What are your branch hours?
Accept: application/json

### 🔹 AcmeBankController – Off-topic question
# Sends a question outside the allowed banking topics.
# The AI responds with a safe fallback message.
GET http://localhost:8080/acme/chat?message=Can you tell me a joke?
Accept: application/json


### 🔹 ArticleController – Blog Post Generator
# Generates a complete 500-word, ready-to-publish blog post.
# You can customize the topic using the `topic` query parameter.
# Endpoint: GET /article/posts/new

GET http://localhost:8080/article/posts/new?topic=The Future of Spring AI
Accept: application/json

### 🔹 VacationPlanController – Unstructured Vacation Plan
# Sends a user prompt to the AI model to generate a vacation plan.
# Returns a free-text recommendation without structured formatting.
GET http://localhost:8080/vacation/unstructured
Accept: application/json

### 🔹 VacationPlanController – Structured Vacation Plan
# Sends a user prompt to the AI model to generate a structured vacation plan.
# Returns an `Itinerary` object containing a list of `Activity` records.
# Each activity includes: activity name, location, day, and time.
# Endpoint: GET /vacation/structured
# Query Parameter:
#   - destination (optional): The destination city. Default is "Cleveland, OH".

GET http://localhost:8080/vacation/structured?destination=Montreal,CA
Accept: application/json


### 🔹 ImageDetection – Image to Text
# Sends a JPEG image to the AI model and returns a textual description.

GET http://localhost:8080/image-to-text
Accept: application/json

### 🔹 ImageGeneration – Generate Image from Prompt
# Generates an image using the OpenAI DALL-E model based on a text prompt.
# Query Parameter:
#   - prompt (optional): Text description of the desired image. Default: "A beautiful sunset over mountains".
GET http://localhost:8080/generate-image
Accept: application/json

### 🔹 RAG Models – Retrieve Model Information
# Uses your VectorStore + ChatClient to answer the query.
# Query Parameter:
#   - message (optional): The question the user asks.
#                         Default → "Give me a list of all the models from OpenAI along with their context window."

GET http://localhost:8080/rag/models
Accept: application/json

### 🔹 DateTime Tools – Get Tomorrow's Day
# Sends a prompt to the AI to determine tomorrow's date.
# The AI can dynamically call the DatTimeTools tool to get the current datetime.
GET http://localhost:8080/tools
Accept: application/json


### 🔹 Task Management – AI Tool Calling
# Interact with the TaskManagementTools through natural-language prompts.
# The AI will automatically detect:
#   - Task creation
#   - Status updates
#   - Task reassignment
# and then call the appropriate tool method.


### ➤ 1. Create a New Task
# "Create a task titled 'Fix login bug' with description 'Investigate and fix authentication issue' assigned to Omar."
GET http://localhost:8080/tasks?message=Create a task titled 'Fix login bug' with description 'Investigate and fix authentication issue' assigned to Omar
Accept: application/json

### ➤ 2. Update Task Status
# "Mark task 1 as COMPLETED."
GET http://localhost:8080/tasks?message=Mark task 1 as COMPLETED
Accept: application/json

### ➤ 3. Reassign a Task
# "Reassign task 1 to Leila."
GET http://localhost:8080/tasks?message=Reassign task 1 to Leila
Accept: application/json

### ➤ 4. Natural Prompts (AI auto-detects)
# "Change the status of task 2 to IN_PROGRESS."
GET http://localhost:8080/tasks?message=Change the status of task 2 to IN_PROGRESS
Accept: application/json

### ➤ 5. Task Not Found Example
# "Complete task 99."
GET http://localhost:8080/tasks?message=Complete task 99
Accept: application/json



### 🔹 City Info – AI-Powered City Information with Weather Integration
# Sends a natural language query to the AI to get city information.
# The AI will automatically call the `currentWeatherFunction` if relevant.
# Query Parameter:
#   - message (required): The user's city-related question.
# Example queries:
#   - "Tell me about Paris"
#   - "What's the weather like in New York?"

GET http://localhost:8080/cities?message=Tell me about Paris
Accept: application/json

###
GET http://localhost:8080/cities?message=What's the weather like in New York?
Accept: application/json


