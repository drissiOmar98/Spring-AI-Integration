spring:
  application:
    name: spring-ai-03-chat-with-claude
  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY}
      chat:
        options:
          model: claude-opus-4-0
          temperature: 0.7

logging:
  level:
    org.springframework.ai.chat.client.advisor: DEBUG
